# Active Work

Open investigations and work-in-progress records. Write-once per record.

## Entries

Add new files here via capture skill. Close with `git mv` to `kb/history/`.

(No entries yet — populated during agent usage.)
